---
import Container from '@components/ui/Container.astro';
import Button from '@components/ui/Button.astro';
---

<section class="about-teaser">
  <Container narrow>
    <div class="about-content reveal">
      <p class="about-text">
        I look for the frame where a figure changes the scale of everything around it. Mountains, coastlines, and a reason to wake up at 3am.
      </p>
      <Button href="/about" variant="ghost">About &rarr;</Button>
    </div>
  </Container>
</section>

<style>
  .about-teaser {
    padding: var(--space-3xl) 0;
    border-top: 1px solid var(--color-border);
  }

  .about-content {
    text-align: center;
  }

  .about-text {
    font-size: var(--text-xl);
    font-weight: 300;
    line-height: 1.5;
    color: var(--color-text-muted);
    max-width: 700px;
    margin: 0 auto var(--space-lg);
  }
</style>
