---
import PageLayout from '@layouts/PageLayout.astro';
import Container from '@components/ui/Container.astro';
---

<PageLayout title="About" description="About the photographer">
  <Container narrow>
    <section class="about-page">
      <div class="about-header reveal">
        <h1>About</h1>
      </div>

      <div class="about-body">
        <div class="about-text reveal">
          <p>
            I photograph people in landscapes. The intersection matters more to me
            than either subject alone — a figure on a ridgeline changes the scale of
            everything around it. Most of my work happens in mountains, coastlines,
            and terrain that takes planning to reach.
          </p>
          <p>
            I look for textures, leading lines, and light that reveals structure.
            I shoot deliberately. Fewer frames, more time reading a scene before
            picking up the camera. The goal is images that hold specific information:
            where this was, what it felt like to stand there, why it mattered.
          </p>
          <p>
            Workshops in Montana, the Pyrenees, and the Dolomites sharpened how I
            think about composition and story. The consistent lesson: get closer,
            commit to the frame.
          </p>
          <p>
            I write about photography and process on
            <a href="/blog">Substack</a>.
          </p>
        </div>

        <div class="about-gear reveal">
          <h2>Kit</h2>
          <ul class="gear-list">
            <li>Canon EOS R5 Mark II</li>
            <li>Canon RF 35mm f/1.4 L VCM</li>
            <li>Canon RF 24-70mm f/2.8 L IS</li>
            <li>Canon RF 70-200mm f/2.8 L IS</li>
            <li>Leica Q2</li>
            <li>DJI Mavic 3 Pro</li>
            <li>Canon EOS 3 (film)</li>
          </ul>
        </div>

        <div class="about-contact reveal">
          <h2>Get in Touch</h2>
          <p>
            For prints, collaborations, or commissions —
            <a href="mailto:corbin@heist.studio">corbin@heist.studio</a>
          </p>
        </div>
      </div>
    </section>
  </Container>
</PageLayout>

<style>
  .about-page {
    padding: var(--space-xl) 0 var(--space-3xl);
  }

  .about-header {
    margin-bottom: var(--space-xl);
  }

  .about-header h1 {
    font-weight: 300;
  }

  .about-text {
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
  }

  .about-text p {
    font-size: var(--text-lg);
    font-weight: 300;
    line-height: 1.7;
    color: var(--color-text-muted);
  }

  .about-text a {
    color: var(--color-text);
    text-decoration: underline;
    text-underline-offset: 3px;
    transition: color var(--duration-fast) ease;
  }

  .about-text a:hover {
    color: var(--color-interactive-hover);
  }

  .about-gear {
    margin-top: var(--space-2xl);
    padding-top: var(--space-xl);
    border-top: 1px solid var(--color-border);
  }

  .about-gear h2 {
    font-weight: 300;
    font-size: var(--text-xl);
    margin-bottom: var(--space-sm);
  }

  .gear-list {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-xs);
    color: var(--color-text-muted);
    font-size: var(--text-base);
  }

  @media (min-width: 640px) {
    .gear-list {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  .about-contact {
    margin-top: var(--space-2xl);
    padding-top: var(--space-xl);
    border-top: 1px solid var(--color-border);
  }

  .about-contact h2 {
    font-weight: 300;
    font-size: var(--text-xl);
    margin-bottom: var(--space-sm);
  }

  .about-contact p {
    color: var(--color-text-muted);
  }

  .about-contact a {
    color: var(--color-text);
    text-decoration: underline;
    text-underline-offset: 3px;
  }
</style>
